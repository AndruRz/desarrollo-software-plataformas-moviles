import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'practice-02-details-contacts',
  webDir: 'dist'
};

export default config;
